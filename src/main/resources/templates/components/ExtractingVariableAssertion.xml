def failedRegexExtractions = vars.entrySet()&#xa;    .findAll { it.value.equals(it.key + "_NOT_FOUND") &#x26;&#x26; !(vars[it.key + '_matchNr'] &#x26;&#x26; vars[it.key + '_matchNr'] != '0') }&#xa;    .collect { it.key }&#xa;    .each { vars.remove(it)&#xa;} &#xa;if (failedRegexExtractions) {&#xa;    AssertionResult.setFailureMessage(&quot;Couldn't extract value for the following variables: ${failedRegexExtractions}&quot;);&#xa;    AssertionResult.setFailure(true);&#xa;}
